func main() -> int {
    // Create a new Logger class
	logger = new Logger("test.log") // If no arguments are given, the it will log to standard output

    // Log "Hello, world!" to test.log
	logger.log("Hello, world!")
    return 0
}
