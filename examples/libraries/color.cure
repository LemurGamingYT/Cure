// 'color' provides a simple interface and standardized way for representing colors
use "color"


func main() -> int {
    // Instead of having multiple colors, you now just have one!
    test_color = new Color(255, 255, 255) // You can also create a new Color using a hex string
    print(test_color) // Print out the RGB color
    return 0
}
