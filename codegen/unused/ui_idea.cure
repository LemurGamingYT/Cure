use "ui"


func main() -> int {
    window = new Window("My Title", 800, 600)
    points = {{50, 50}, {100, 100}}
    while (!window.should_close) {
        window.draw_point(points, 10)
        window.draw_text("Hello Cure!")
        window.update()
    }

    return 0
}
